{
  "name": "js-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node src/server.js",
    "lint": "eslint .",
    "format": "npx prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "export $(grep -E '^(dev_user|dev_password|db_name)=' .env | xargs) && docker exec -i $(docker compose ps -q db) mysql -u$dev_user -p$dev_password $db_name < sql/seed.sql",
    "seed:postgres": "echo 'Postgres not supported'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.2.1",
    "express-rate-limit": "^8.4.1",
    "firebase": "^12.10.0",
    "firebase-admin": "^13.6.0",
    "mysql2": "^3.12.0",
    "stripe": "^22.1.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@trivago/prettier-plugin-sort-imports": "6.0.1",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "prettier": "3.7.4"
  }
}
